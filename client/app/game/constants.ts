// constants.ts
export const WIDTH = 800;
export const HEIGHT = 800;
export const ROWS = 8;
export const COLS = 8;
export const SQUARE_SIZE = WIDTH / COLS;

export const RED: [number, number, number] = [255, 0, 0];
export const WHITE: [number, number, number] = [255, 255, 255];
export const BLACK: [number, number, number] = [0, 0, 0];
export const BLUE: [number, number, number] = [0, 0, 255];
export const GREY: [number, number, number] = [128, 128, 128];

// Adjust this path depending on your asset management (e.g., require() in React Native)
export const CROWN = 'assets/crown.png';
